<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myappartment.my_appartment">
    android:name="android.support.multidex.MultiDexApplication"
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".requestsList"
            android:exported="true" />
        <activity android:name=".rentingReg" />
        <activity android:name=".renterReg"
            android:exported="true"/>
        <activity android:name=".renterRrequest" />
        <activity android:name=".test" />
        <activity
            android:name=".rentingList"
            android:exported="true" />
        <activity android:name=".createRequest"
            android:exported="true"
            ></activity>
    </application>

</manifest>